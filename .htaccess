Options -Indexes -MultiViews
DirectoryIndex index.html index.php
AddDefaultCharset UTF-8

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Internal project folders and source materials should not be public.
  RewriteRule ^(?:data|wokiee|Imagenes_identidad|_codex_doc_render|\.git|\.agents|\.codex)(?:/|$) - [F,L,NC]

  # Block project documents that are useful locally but should not be served.
  RewriteRule \.(?:md|doc|docx|xls|xlsx|ppt|pptx|sql|log|bak)$ - [F,L,NC]

  # Keep real files and directories working normally.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Single page fallback: friendly internal routes load the main page.
  RewriteCond %{REQUEST_URI} !\.[a-zA-Z0-9]{2,6}$
  RewriteRule ^ index.html [L]
</IfModule>

<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>
